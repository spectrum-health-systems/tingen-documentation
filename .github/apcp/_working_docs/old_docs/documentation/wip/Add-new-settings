# Add new settings

## Configuration setting

Configuration settings are settings that users should be able to modify.

1. Add the property to TingenConfiguration.Properties.cs
2. Add settings values to TigenConfiguration.Build()

## Session settings

1. Add the property to TingenSession.Properties.cs
2. Add settings values to TigenSession.Build()

## Framwork settings

1. Add the property to TingenFramework.Properties.cs
2. If the property is a path, add it to Framework.Catalog.cs
3. Add settings values to TigenFramework.Build()